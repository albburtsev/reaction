.page
    background_dark_primary()
    display: flex
    flex-direction: column
    justify-content: center
    min-height: 100%

.content
    max-width: 30rem
    margin: 0 auto
    text-align: center

.name
    font_size_display()
    color_text_primary()

    &::first-letter
        text-transform: uppercase

.description
    font_size_regular()
    color_light_primary()
    margin: 1rem 0
