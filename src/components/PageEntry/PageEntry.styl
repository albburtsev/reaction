.page
    page_layout()
    text-align: center

.name
    font_size_display()
    color_text_primary()

    &::first-letter
        text-transform: uppercase

.description
    font_size_regular()
    color_light_primary()
    margin: 1rem 0
